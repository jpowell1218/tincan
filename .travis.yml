language: ruby
cache: bundler

rvm:
  - 2.0.0
  - 2.1.0

services:
  - redis-server

script: 'bundle exec rake'

notifications:
  email:
    recipients:
      - ben@captainu.com
    on_failure: change
    on_success: never
